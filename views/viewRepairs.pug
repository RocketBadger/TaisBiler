h2 Reparationer
table.listTable
    each rep in car.repairs
        tr
            td #[strong Reparation:] #{rep.repair}
                br
                |#[strong Dato:] #{rep.date.toDateString()}
                br
                if rep.repaired
                    |#[strong Repareret:] Ja
                else
                   |#[strong Repareret:] Nej
            td
                a(href="/biler/redigerReparation/" + car._id, class='emojiLink') ✏️