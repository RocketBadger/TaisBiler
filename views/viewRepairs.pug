h2 Reparationer
- let carRep = car.repairs.sort(function(a,b){
- let c = new Date(a.date);
- let d = new Date(b.date);
- return c-d;
- })
table.listTable
    each rep in car.repairs
        tr
            td.listTD #[strong Reparation:] #{rep.repair}
                br
                |#[strong Dato:] #{rep.date.toLocaleDateString()}
                br
                if rep.repaired
                    |#[strong Repareret:] Ja
                else
                   |#[strong Repareret:] Nej
            td.listTD
                a(href="/redigerReparation/" + rep._id, class='emojiLink') ✏️