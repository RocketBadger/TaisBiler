h2 Reparationer
table.listTable
    each rep in car.repairs
        tr
        table.listTable
            td #[strong Reparation:] #{rep.repair}
                br
                |#[strong Dato:] #{rep.date.toDateString()}
                br
                if rep.repaired
                    |#[strong Repareret:] Ja
                else
                   |#[strong Repareret:] Nej
            td
                a(href="/biler/redigerReparation/" + rep._id, class='emojiLink') ✏️